# Copy to .env and fill in. Never commit .env.
VAULT_ADDR=http://127.0.0.1:8200
VAULT_TOKEN=root
VAULT_DEV_ROOT_TOKEN_ID=root
VAULT_DEV_LISTEN_ADDRESS=0.0.0.0:8200
VAULT_INTEGRATION=false

VAULT_AUTH_METHOD=token
VAULT_NAMESPACE=
VAULT_IGNORE_SSL=false

VAULT_ROLE_ID=
VAULT_SECRET_ID=
VAULT_APPROLE_MOUNT=approle

VAULT_KV_MOUNT=secret
VAULT_KV_VERSION=v2
VAULT_AWS_MOUNT=aws
VAULT_AZURE_MOUNT=azure
VAULT_GCP_MOUNT=gcp
VAULT_DATABASE_MOUNT=database
VAULT_SSH_MOUNT=ssh

# Librería Python (Settings → Conectores). No mezclar con el token de lab de arriba.
# RUVIC_HASHICORP_VAULT_VAULT_URL=http://127.0.0.1:8200
# RUVIC_HASHICORP_VAULT_AUTH_MODE=token
# RUVIC_HASHICORP_VAULT_TOKEN=
# RUVIC_HASHICORP_VAULT_KV_MOUNT=secret
# RUVIC_HASHICORP_VAULT_KV_VERSION=v2
